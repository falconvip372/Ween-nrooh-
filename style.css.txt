/* استخدام خط Inter كخط أساسي */
body {
    font-family: 'Inter', sans-serif;
    background-color: #f7f7f7; /* bg-gray-50 */
}

/* إخفاء الصفحات غير النشطة */
.page {
    display: none;
}

/* إظهار الصفحة النشطة حالياً */
.page.active {
    display: block;
}

/* شريط الإشعارات */
#notification {
    position: fixed;
    top: 20px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 1000;
    transition: all 0.3s ease-out;
}

/* زر رفع الصورة */
input[type="file"]::file-selector-button {
    margin-right: -1rem; margin-left: 1rem; background-color: #f0f4ff; color: #4338ca;
    padding: 0.5rem 1rem; border: none; border-radius: 0.5rem; font-weight: 600;
    cursor: pointer; transition: background-color 0.2s;
}
input[type="file"]::file-selector-button:hover { background-color: #e0e7ff; }

/* شاشة التحميل */
#loading-overlay {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    background-color: rgba(255, 255, 255, 0.8);
    display: flex; justify-content: center; align-items: center;
    z-index: 9998; backdrop-filter: blur(4px);
}
.loader {
    border: 5px solid #f3f3f3; border-top: 5px solid #4338ca;
    border-radius: 50%; width: 50px; height: 50px;
    animation: spin 1s linear infinite;
}
@keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }

